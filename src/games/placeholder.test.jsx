// Placeholder aggregator to prevent react-scripts from picking up stray top-level test files
// This file intentionally contains a single trivial test to satisfy test discovery.
test('placeholder aggregator', () => {
  expect(true).toBe(true);
});
